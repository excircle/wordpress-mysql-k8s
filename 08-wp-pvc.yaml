---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ${WP_PV_NAME}
  labels:
    app: ${APP}
    tier: mysql
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: ${WP_DISK_SIZE}Gi